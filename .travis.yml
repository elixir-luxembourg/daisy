language: python

python:
  - "3.6"
  - "3.5"
  
services:
  - docker
  
install:
  - docker-compose build
  - docker-compose up -d

script:
  - docker-compose exec -e DJANGO_SETTINGS_MODULE=elixir_daisy.settings_compose web python setup.py pytest
