language: python

python:
  - "3.6"
  - "3.7"
  - "3.5"
  
services:
  - docker
  
install:
  - docker-compose build
  - docker-compose up -d
  
script:
  - docker-compose exec web python setup.py pytest
